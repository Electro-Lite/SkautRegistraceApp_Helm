apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SkautRegistraceChart.fullname" . }}-test-connection"
  labels:
    {{- include "SkautRegistraceChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SkautRegistraceChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
