apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SkautRegistraceChart.fullname" . }}-test-connection"
  labels:
    {{- include "SkautRegistraceChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.spring.service.name }}:{{ .Values.spring.service.port }}']
  restartPolicy: Never
