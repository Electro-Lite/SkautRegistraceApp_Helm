apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: skaut-spring-http-route
spec:
  parentRefs:
    - name: skaut-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: spring-service
          port: 8080
